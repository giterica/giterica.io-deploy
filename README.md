giterica.io-deploy
==================

giterica.io deployment with Docker Swarm. docker-stack.yml and Caddy config for external HTTPS. 

Deployment
----------

First time:

```bash
docker swarm init
./init
```

Stack deployment

```bash
./deploy
```